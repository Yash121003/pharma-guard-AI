# Dev-only tools -- not needed to run the application itself.
# Install with: pip install -r requirements-dev.txt (after requirements.txt)

ruff==0.8.4          # linting
reportlab==4.2.5      # used only to (re)generate the sample PDF complaint fixture in tests/sample_files/
